name: post_repository

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^2.5.1
  firebase_storage: ^10.0.3
  rxdart: ^0.27.1
  equatable: ^2.0.3
  firebase_core: ^1.6.0